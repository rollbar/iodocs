include highlight

- var extra = [];
- if (query.params.page)
    - extra.push('page=' + query.params.page)
- if (extra.length > 0)
    - extra = '&' + extra.join('&')

div
    h2 Curl Example
    pre
        code
            curl #{baseUrl}/item/#{query.params.item_id || ':item_id'}/instances/?access_token=#{accessToken}#{extra}
